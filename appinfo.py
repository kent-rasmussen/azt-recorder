"""
A-Z+T Recorder — part of the A-Z+T suite of linguistic tools.

Canonical app identity constants. Import from here rather than
hard-coding names elsewhere in the codebase.
"""

APP_NAME = 'A-Z+T Recorder'          # display name (human-readable)
APP_SLUG = 'azt-recorder'             # URL/package slug
APP_TAGLINE = 'Record My Wordlist'    # shown on welcome screen & window title
APP_USER_AGENT = 'AZTRecorder'        # HTTP User-Agent prefix (no spaces)
